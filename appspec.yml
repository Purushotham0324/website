--- 
files: 
  - 
    destination: /var/www/html/
    source: /index.hml
hooks: 
  AfterInstall: 
    - 
      location: |-
          sed -i -e 's/
          $//' scripts/start.sh
      runas: root
      timeout: 300
  BeforeInstall: 
    - 
      location: |-
          sed -i -e 's/
          $//' scripts/install_dependencies.sh
      runas: root
      timeout: 300
os: linux
permissions: 
  - 
    group: root
    mode: 777
    object: /var/www/html
    owner: root
version: 0.0
